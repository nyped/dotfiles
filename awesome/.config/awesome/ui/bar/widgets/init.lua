return {
    battery = require("ui.bar.widgets.battery"),
    brightness = require("ui.bar.widgets.brightness"),
    class = require("ui.bar.widgets.class"),
    hwmon = require("ui.bar.widgets.hwmon"),
    notifications = require("ui.bar.widgets.notifications"),
    systray = require("ui.bar.widgets.systray"),
    volume = require("ui.bar.widgets.volume"),
    clock = require("ui.bar.widgets.clock"),
    taglist = require("ui.bar.widgets.taglist"),
    tasklist = require("ui.bar.widgets.tasklist"),
    layoutbox = require("ui.bar.widgets.layoutbox"),
}

-- vim: filetype=lua:expandtab:shiftwidth=4:tabstop=8:softtabstop=4:textwidth=80
